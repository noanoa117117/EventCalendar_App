# 共有イベントカレンダー＋空き時間アプリ 要件定義書 v0.3（Phase 1-2 実装指示）

v0.1 draft を元に、/grill-me セッションで確定した決定事項を反映。v0.3では実装時に解釈が分かれやすい認可・データ境界・操作仕様を明文化した。
本ドキュメントは **Phase 1〜2 の実装範囲** を対象とする。Phase 3 以降（画面①③、レイアウト統合）は別途着手する。

## 1. 目的とコンセプト

仲間内で遊ぶ予定を立てるためのWebアプリ。

従来の「日程を決めてから調整さんで参加者を募る」流れではなく、各メンバーがあらかじめ「暇な時間帯」を登録しておき、その空き時間に対して「この時間、イベントしませんか？」と誘うスタイルを取る。

空き時間の登録操作の手軽さが本アプリの生命線。ここが面倒だと誰も登録せず、システム全体が死ぬ。**Phase1-2はこの核心操作（ペイント登録）を実際に作って触り心地を確認するための区切り。**

重要な前提
- 空き時間を見た ≠ 参加意思。空き時間の登録と、イベントへの参加表明は別のアクションとして明確に分ける（主語が別＝企画者と参加者は別の行為主体）。
- 仲間内利用のため、カレンダーの閲覧権限制御（ACL）は行わない。ログインユーザーは全員のカレンダーを閲覧できる。

## 2. スコープ

### 2.1 今回実装する（Phase 1-2）
- Google認証によるログイン
- **ホワイトリストによるアクセス制限**（許可メールアドレスを管理者が手動追加。ホワイトリスト外のGoogleアカウントはログイン不可）
- ニックネーム管理（本名の秘匿）
- DBスキーマ全体（profiles / availability_presets / availability_slots / events / event_participants）— events系テーブルはPhase1で作成するが、Phase1-2ではUIから触らない
- 画面②：個人の空き時間の閲覧・登録（プリセット + ペイント操作）

### 2.2 今回は実装しない（Phase 3以降）
- 画面①：共有イベントカレンダー、イベントへの参加表明
- 画面③：イベント企画・共通空き時間算出（ヒートマップ）
- 3ペイン統合レイアウト・レスポンシブ調整（モバイルの下部タブを含む）
- 画面②への確定イベントの表示上書き（Phase3で画面①が実装されて初めて意味を持つ機能のため、Phase2時点ではイベントデータが存在せず対象外）

### 2.3 将来拡張候補（完全にスコープ外）
- Googleカレンダーとの双方向同期
- Discord等の外部サービスへの通知・リマインド連携
- 繰り返しイベント
- メール通知
- 画像・ファイル添付

## 3. 技術スタック

| 領域 | 採用 | 理由 |
|---|---|---|
| フレームワーク | Next.js 15 (App Router) / TypeScript | フロント・API・認証を1リポジトリで完結できる |
| UI | Tailwind CSS + shadcn/ui | カレンダーの独自グリッドを組みやすい |
| DB / 認証 | Supabase (PostgreSQL + Auth) | Google OAuthが設定のみで動く |
| ORM | Supabase Client | 小規模なので薄い層で十分 |
| ホスティング | Vercel | Next.jsとの相性、無料枠で足りる規模 |
| 日付処理 | date-fns / date-fns-tz | moment禁止 |

- カレンダーUIはFullCalendar等のライブラリを使わず自前実装。30分スロットのペイント操作など独自要件が多いため。
- タイムゾーンは Asia/Tokyo 固定。DBには timestamptz で保存し、表示時にJSTへ変換。

## 4. アクセス制限（ホワイトリスト）

- 許可されたメールアドレスの一覧をDB（例: `allowed_emails` テーブル、または管理用テーブル）で管理。
- メールアドレスは前後空白を除去し、小文字化した値で照合・一意管理する。無効化されたエントリは許可しない。
- Google OAuthログイン成功後、認証されたメールアドレスがホワイトリストに存在するかをチェック。
  - 存在しない場合：ログインを拒否し、アクセス不可の旨を表示（アプリ内画面には遷移させない）。
  - 存在する場合：通常のログインフローへ（ニックネーム未設定なら設定画面へ強制遷移）。
- ホワイトリスト判定は画面遷移だけでなく、保護対象のAPI・DBアクセスでも必ず行う。直接URLやクライアントからのDBアクセスで、ホワイトリスト外ユーザーがアプリデータを取得・更新できてはならない。
- OAuthの認証レコードが作成されうることと、アプリ利用を許可することは別とする。ホワイトリスト外ではprofiles・初期プリセットを作成せず、アプリ内データへのアクセスを一切許可しない。
- ホワイトリストの追加・削除は管理者がDBを直接操作する運用でよい（管理画面UIはPhase1-2のスコープ外）。

## 5. データモデル

### allowed_emails
| カラム | 型 | 備考 |
|---|---|---|
| id | uuid (PK) | |
| email | text (unique, not null) | 小文字化・前後空白除去後の値を保存 |
| is_enabled | boolean (not null) | falseならアクセス不可 |
| created_at | timestamptz (not null) | |

ホワイトリストに必要なテーブルとしてPhase1で作成する。メールアドレスそのものは認可目的に限りこのテーブルに保持し、アプリの表示・プロフィール情報には利用しない。

### profiles
| カラム | 型 | 備考 |
|---|---|---|
| id | uuid (PK) | auth.users.id と同一 |
| nickname | text (unique, not null) | アプリ内で表示される唯一の名前。前後空白除去後で一意 |
| color | text (not null) | カレンダー上の識別色。指定パレットから自動設定 |
| created_at | timestamptz (not null) | |

本名秘匿の要件: Googleアカウントの表示名・メールアドレス・プロフィール画像は、アプリ内のいかなる画面にも出さない。DBにも保存しない（auth.users側に留める）。初回ログイン時にニックネーム設定を必須とし、未設定ユーザーは他画面へ遷移できない。

ニックネームは1〜20文字、空白のみは不可とする。大文字・小文字のみが異なるニックネームは同名として扱う。作成・更新時に重複した場合は保存せず、利用者に再入力を促す。ニックネームと初期プリセットの作成は、許可済みユーザーに対して一度だけ行われるよう冪等に実装する。

### availability_presets（暇な時間帯パターン）
| カラム | 型 | 備考 |
|---|---|---|
| id | uuid (PK) | |
| user_id | uuid (FK) | |
| label | text | 例:「平日夜」「休日昼」「終日OK」 |
| start_time | time | 30分境界（:00 または :30） |
| end_time | time | 同上 |
| color | text | |
| sort_order | int | |

**新規ユーザーには初期プリセットを自動作成する**（例:「平日夜 20:00-24:00」「休日昼 13:00-18:00」）。ユーザーは自由に編集・削除・追加できる。

開始・終了時刻は30分境界で、開始と終了が同一にはできない。日をまたぐプリセットは扱わない。ただし初期値の「24:00」は、その日の終端を表す表示上の値とし、保存時は `00:00`（翌日開始）として扱う。プリセットの削除は、それを過去に適用して作成済みの空き時間を削除・変更しない。

### availability_slots（実際の空き時間）
| カラム | 型 | 備考 |
|---|---|---|
| id | uuid (PK) | |
| user_id | uuid (FK) | |
| date | date | |
| start_time | time | 30分境界 |
| end_time | time | 30分境界 |
| preset_id | uuid (FK, nullable) | どのプリセット由来か。手動登録ならnull |

- 保存は時間帯レンジで行う（30分行を大量に持たない）。
- 同一ユーザー・同一日で範囲が重複・隣接する場合は、保存時にマージして正規化する。
- `start_time` と `end_time` は30分境界とし、同一値は不可。`end_time = 00:00` は当該日の終端（翌日00:00）を表す。これ以外の日またぎ範囲は登録不可とする。
- 空き時間の正規化で複数の登録元が混ざる場合、マージ後レコードの `preset_id` は `null` とする。したがって `preset_id` は由来の表示・追跡には使わず、適用直後の補助情報としてのみ扱う。
- プリセットの適用は、対象日の同一時刻範囲を「空き」にする冪等操作とする。解除時に別の登録経路と重なる範囲をどう扱うかは、§9 の要決定事項に従う。
- 共通空き時間の算出（Phase3）時のみ、メモリ上で30分スロットに展開する（`expandToSlots()` ユーティリティを用意）。
- **登録可能期間：当月＋3ヶ月まで**。範囲外の日付への登録操作はUI上で無効化する。
- 過去日は編集不可（表示はする）。
- **イベント参加とavailability_slotsのデータ連動はしない**（Phase3以降、画面②に確定イベントを表示のみ上書きする機能を追加するが、availability_slots自体は変更しない）。

### events / event_participants
Phase1でテーブルのみ作成（DBスキーマとして用意）。Phase1-2のUIからは操作しない。カラム定義はv0.1 draft通り。

- events: id, title, description, start_at, end_at, created_by, status(published/cancelled), created_at
- event_participants: event_id, user_id, status(going/maybe/declined), comment, updated_at
  - レコードなし＝未回答。declinedと未回答を区別するため3値。
  - **イベント作成時、企画で選択したメンバーを自動的に参加者登録することはしない**（参加者ゼロで作成。参加表明は各自の能動的アクション。この方針はPhase3実装時に反映）。

## 6. 画面②：個人の空き時間（Phase1-2 メイン機能）

3カラム構成。

| 位置 | 内容 |
|---|---|
| 左 | メンバー一覧。チェックで対象メンバーのカレンダーを表示切替 |
| 中央 | 月／週カレンダー（Outlook風） |
| 右 | 登録済みの「暇な時間帯」プリセットのトグル一覧 |

### 登録操作（ペイントモード）
1. 右カラムのプリセットトグルを押す → そのプリセットが活性化
2. 活性化中は、中央の月カレンダーの日付をクリック／ドラッグすると、その日にプリセットの時間帯が一括適用される
3. 適用済みの日を再度クリックすると解除
4. トグルをもう一度押す、またはEscでペイントモード終了

月表示のドラッグは、時刻範囲を伸縮する操作ではなく、ポインタが通過した複数の日付に選択中プリセットを適用する操作とする。編集不可の日付はドラッグ中も変更しない。適用済みかどうかは、対象プリセットの全時間帯が空きとして登録済みかで判定する。

この操作の軽さが最優先要件。数クリックで月のカレンダーを埋められること。

### その他
- プリセットの追加・編集・削除画面（ラベル、開始/終了時刻、色）
- 週表示では30分単位での細かい直接ドラッグ登録も可能にする
- 週表示の直接ドラッグでは、ドラッグ開始時の状態に合わせて選択範囲全体を登録または解除する。既存レンジと重なっても、保存後は上記の正規化ルールを満たすこと。
- 他メンバーの空き時間は閲覧のみ（編集不可）
- 過去日は編集不可（表示はする）
- 登録可能期間は当月＋3ヶ月まで（それ以降はUI上で操作不可）
- Phase1-2ではデスクトップでの操作検証を優先する。モバイルのレイアウト・タッチ操作の詳細は、3ペイン統合と合わせてPhase5で定義・実装する。

## 7. 非機能要件
- 対象ブラウザ: 最新版 Chrome / Edge / Safari
- 想定ユーザー数: 20人程度（性能要件は緩い）
- タイムゾーン: Asia/Tokyo 固定
- 未ログイン時・ホワイトリスト外は全画面をログイン/アクセス不可ページへリダイレクト
- Row Level Security等により、ログインユーザーは自分のprofiles・availability_presets・availability_slotsのみ作成・更新・削除できること。他ユーザーの空き時間は閲覧のみとし、Googleの表示名・メールアドレス・プロフィール画像および `allowed_emails` はクライアントから閲覧できないこと。
- 保存失敗時は、画面上で失敗を通知し、未保存状態のまま成功したようには表示しないこと。

## 8. 完了条件（Phase1-2）

以下が一通り動作すること。

1. Googleログインでき、ホワイトリスト外のアカウントは弾かれる
2. 初回ログイン時にニックネーム設定が必須で、未設定では他画面に遷移できない
3. 新規ユーザーに初期プリセットが自動で用意されている
4. 画面②で、プリセットをトグルしてペイントモードに入り、月カレンダー上のクリック／ドラッグで空き時間を一括登録・解除できる
5. 週表示で30分単位の直接ドラッグ登録ができる
6. 他メンバーの空き時間を切り替えて閲覧できる（編集はできない）
7. 過去日・登録可能期間（当月＋3ヶ月）外は編集操作が無効化されている
8. プリセットの追加・編集・削除ができる
9. 直接URL・DB API経由を含め、ホワイトリスト外ユーザーはアプリデータにアクセスできず、ログインユーザーも他人の空き時間を変更できない
10. 同一日の空き時間に重複・隣接した登録を行っても、保存データが正規化され、プリセットの適用・解除が§9で決定した仕様どおりに動作する

## 9. 実装上の前提・要決定事項
- ホワイトリストはDB直編集で管理する。管理画面はPhase1-2では提供しない。
- **要決定: プリセット解除と別経路の登録が重なる場合の扱い。**
  - 推奨: Phase1-2では「空き／空きでない」の最終状態だけを管理し、プリセット解除は重なる空き時間も解除する。登録経路ごとの重ね合わせを持たないため実装・データモデルを単純に保てる。
  - 代替: プリセット由来・手動登録由来を別々に保持し、ほかの経路で空きになっている範囲は解除しない。この場合は `availability_slots` のデータモデルを、由来を保持できる形に変更する必要がある。

## 10. Phase3以降への引継ぎ要件（v0.1）
- 画面①（共有イベントカレンダー）は、月／週表示、イベント詳細、参加表明、主催者による編集・キャンセルを提供し、ログイン直後のデフォルト画面とする。
- 画面③（イベント企画）は、選択メンバーの空き時間を30分単位で集計し、共通空き時間をヒートマップ表示してイベント作成へつなげる。
- 画面①・②・③はそれぞれ独立した画面として遷移する。ログイン後の標準画面は①（`/events`）とし、②（`/availability`）と③（`/planning`）は各画面のナビゲーションから開く。モバイルで3画面を同時表示しない。
- ③でイベントを登録した後は①（`/events`）へ遷移する。イベントの参加表明は空き時間の閲覧・登録とは連動させず、①のイベント詳細で各ユーザーが明示的に行う。
